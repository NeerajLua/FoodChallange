import React from 'react';
//import './Header.css'; // Optional for styling

const Header = () => {
  return (
    <header style={{ backgroundColor: '#3f51b5', padding: '0.1rem', color: '#fff' }}>
      <h1>🍴 Food's Restaurant</h1>
    </header>
  );
};

export default Header;
